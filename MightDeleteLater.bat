while(($p=Read-Host "Enter full folder path (or 'exit' to quit)") -ne "exit"){ $clean=$p.Trim(); if($clean -like 'PS *'){ $clean=$clean.Substring(3).TrimStart() }; if($clean.EndsWith('>')){ $clean=$clean.Substring(0,$clean.Length-1).TrimEnd() }; $b=""; $u=""; if($clean.TrimEnd('\').StartsWith($b.TrimEnd('\'),[System.StringComparison]::InvariantCultureIgnoreCase)){ $r=$clean.TrimEnd('\').Substring($b.Length).TrimStart('\'); $e=[uri]::EscapeDataString($r) -replace '%5C','/'; Write-Host ($u + $e) } else { Write-Warning "Path not inside repo base" } }